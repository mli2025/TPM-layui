# arbore TPM · 设备管理系统 · 项目路线图

> 防遗忘清单。所有阶段交付物按勾选推进，**不再承诺"一次性搞定"**。
>
> 需求源：《天台药业设备管理模块 URS 2026.4.29 更新（最终版）》——185 条需求 / 12 个核心模块（见 `天台药业设备管理系统-模块分析.md`）
> 业务参考：活字格 `waes_mes_hamaton`（设备管理模块，现有功能的移植来源）
> 架构参考：`waes_nppms_core`（分层 + Razor + Dapper）
> 视觉参考：[Plane self-hosted](https://github.com/makeplane/plane)（Navigation 2.0）
> 后台能力：**工作流**（OCR/AI agent/通知/定时流），优先借力，减少纯代码实现

---

## ✅ v2.1.0 第一轮交付完成情况（2026-05-30，批次 1-10 已全部 push）

> 一次性交付，供第一轮移交测试；工作流 / OCR / AI agent 仅做存储设计与接入点说明，不在本轮代码范围。
> 数据库补丁：`db-patches/v2.1.0.sql`（幂等，可重复执行；新增物理表 5 张，其余复用 v2.0.0）。

| 批次 | 内容 | 关键产出 | 状态 |
|---|---|---|---|
| 1 | 登录安全 / 角色模板 / 备件CRUD | 会话滑动超时、登录日志、账户锁定、用户组权限并集、8角色模板、Basic_Spare 增删改+审计 | ✅ |
| 2 | 字段级审计 | `AuditService` 反射 diff、`Sys_AuditTrail` 时间线查询页 | ✅ |
| 3 | 通用能力 | 批量导入框架(`IImportHandler`/模板/日志)、报表引擎(`Sys_ReportDef` 受限SELECT+ECharts+导出) | ✅ |
| 4 | 工作流挂接 | `GetByBiz` + 全局 `wf-widget.js`，维修工单接入示例 | ✅ |
| 5 | 维保增强 | 维保标准库(主子)、延期申请审批、资质有效期三色监控 | ✅ |
| 6 | 点检重构 | 点检标准库、计划(列表+月历)、PC执行单逐项填写提交、异常5类处置分流（新表 `Inspect_RecordSub`） | ✅ |
| 7 | 设备档案 | FAT/SAT验收+问题跟踪、盘点(主子)、资产卡片、证书三色时效、标签生成打印、润滑标准+记录 | ✅ |
| 8 | 维修增强 | 工单模板、多设备费用分摊、报警规则+记录、故障看板(状态/类别/趋势/MTTR ECharts) | ✅ |
| 9 | 备件增强 | 多级阈值预警(严重/警告/提示/超储)、生命周期、盘点(主子)、可出库量(预留留接口)（新表4张） | ✅ |
| 10 | 各模块统计报表 | 报表中心预置 计量/特种/安全/能源/备件 共8张只读报表 | ✅ |

> 移动端代码完成；交付文档、工作流编排、AI agent 由其他同事负责，不属本轮。

---

## URS 对标差距总表（2026-05-29）

> 基线现状：24 张表 / 26 个 Controller。已落地 = 设备台账、保养(项目/模板/工单含看板·日历·甘特·派工·状态机)、维修(工单·派工·三方确认)、点检(偏只读)、备件(主数据·库存·出入库)、系统管理(用户/角色/部门/菜单/按钮/设置/版本/员工)、通用附件、操作日志(仅写入)、移动端(/m 保养·点检·维修)。

| URS 模块 | 现状 | 主要缺口 | 工作流 借力点 | 工作包 |
|---|---|---|---|---|
| 一、用户权限 401-410 | 部分 | 账户锁定、会话超时、用户组、登录日志、8种角色模板、2FA | — | WP-A |
| 二、审计追踪 301-306 | 部分(仅写入) | 查询页、字段级旧值/新值、变更时间线、只追加约束 | — | WP-A |
| 三、设备档案 501-532 | 部分 | FAT/SAT验收、盘点、标签、知识库、文档版本、证书到期、资产卡片、状态变更审批、智能解析 | OCR回填、AI知识库 | WP-B |
| 四、设备维修 601-610 | 部分 | 工单模板、多设备费用分摊、报警规则、故障看板、统计分析、知识库 | 异常触发工单、AI知识库 | WP-C |
| 五、点巡检 701-706 | 弱(只读) | 标准审批、计划日历、移动执行拍照提交、处置分流、智能优化 | 频率智能优化、推送 | WP-D |
| 六、设备维保 801-809 | 部分 | 三色警示、延期申请、资质监控、维保标准独立库、自动生成计划 | 定时生成、到期提醒 | WP-E |
| 七、特种设备 901-905 | **无** | 全新模块：台账+检验计划+任务执行+历史 | 60/30天提醒、证书识别 | WP-F |
| 八、安全附件 1001-1006 | **无** | 全新模块：台账+看板+检定计划+任务跟踪+联动 | 到期提醒、证书识别 | WP-G |
| 九、计量器具 1101-1117 | 几乎无 | 全新模块：档案+台账+出入库+校准计划/记录+送外检+报表+电子签名 | 提醒、证书建档 | WP-H |
| 十、备件 1201-1210 | 部分 | 主数据编辑、阈值预警、需求预测、生命周期、盘点、扫码、报表 | 预警、需求预测 | WP-I |
| 十一、能源 1301-1306 | **无** | 全新模块：介质配置+采集+实时仪表板+报警+统计(依赖采集) | 采集流、报警推送 | WP-J |
| 十二、系统平台 201-217 | 部分 | 通知引擎、工作流引擎、报表引擎、批量导入、备份恢复 | 通知多渠道、定时 | WP-A/K |

完整 185 条逐条对照见 `天台药业设备管理系统-模块分析.md`。

---

## 0. 已锁定决策（2026-05-12 / 2026-05-29 更新）

| 项 | 选型 | 备注 |
|---|---|---|
| 后端框架 | .NET 8 ASP.NET Core MVC + Razor | 保留现有 4 项目分层 |
| ORM | Dapper | 自实现 `IRepository<T>` + `IUnitWork` |
| 数据库 | SQL Server `TPM` | `db-schema-v2.sql`（bigint IDENTITY，含种子） |
| 数据库连接 | `appsettings.Development.json` | 已 .gitignore；公开仓不会泄露 |
| 认证 | Token + Cookie + 模块按钮权限 | `AuthApp` + `Sys_User/Role/Module/ModuleButtons` |
| 前端 CSS | **Tailwind CSS 3.x** | 仅 AppShell/登录等壳层；业务页保留 layui |
| 前端 JS | **AlpineJS** | 仅壳层；业务页仍用 layui + jQuery |
| 业务表格 | **Layui 2.9.13 + layui-soul-table + `SoulGrid` 封装** | 去重下拉筛选/固定列/列显隐/排序/前端导出(所见即所得)；高频档案页用前端全量模式（2026-05-30 定）|
| 图标 | **Lucide** | 替换 font-awesome |
| 字体 | **Inter** + 系统中文回退 | |
| 主色 | **`#1F4D3B`** 森林绿 | 对齐 arbore logo |
| Logo | `arbore_logo.png` | 登录大图 + 顶栏 28px + favicon |
| 视觉对标 | Plane Navigation 2.0 | 深色侧栏 + 顶栏 + tab pill + 用户 dropdown |
| 后台编排 | **工作流**（webhook + 定时 + AI agent） | OCR/智能解析/通知/定时/AI问答优先走 工作流 |

---

## 1. 项目骨架（已完成 ✓）

- [x] 解决方案 4 项目分层（Web / App / Repository / Infrastructure）
- [x] `[Table]` Domain 实体（当前 24 个：Sys 11 / Basic 2 / Facility 8 / Spare 3）
- [x] `IRepository<T>` + `IUnitWork` + `BaseApp<T>` + `BaseController`
- [x] `AuthApp` 登录 / 登出 / Token 缓存 / 模块按钮权限注入
- [x] `db-schema-v2.sql` DDL（bigint IDENTITY，含 admin/菜单/设置/版本种子）
- [x] Serilog 日志 + 全局异常 middleware + `Sys_OperationLog` 写入
- [x] 编译 0 错 0 警告

## 2. 待清理

- [x] ~~删除 `Views/Home/Index.cshtml` `admin / 123456` 提示~~（已整页重写为 AppShell 入口）
- [ ] 删除 `device-mgmt-layui-v1/` 目录（demo placeholder，已无用）
- [ ] 决策：是否把 `device-mgmt-layui-v2/` 重命名为 `arbore-tpm/`（**待用户拍板**）
- [ ] 遗留子表页（`Facility_BillSub` / `Facility_RepairBillSub` / `Facility_TheTemplateSub`）未进菜单，迁移完成后移除
- [ ] 老 layui / select2 / inputmask 资源（~30MB）业务页全部用 Tailwind 重写后移除

---

## P0 框架壳（**已完成 ✓**）

> 目标：**没有任何业务页面也能跑起来**的完整应用骨架。Plane Nav 2.0 风格。已全部交付。

- [x] **P0.1 全局基础设施**：Tailwind / AlpineJS / Lucide / Inter，CSS 变量调色板（`#1F4D3B` 11 档），`app.css` + `app.js`（cookie/ajax-token/toast/theme），全局 `x-cloak`
- [x] **P0.2 登录页**：`_LayoutLogin.cshtml` 全 Tailwind 化，AJAX `POST /Account/DoLogin`（密码不进 URL），已登录跳首页
- [x] **P0.3 AppShell**：`_Layout.cshtml` 三栏 grid，顶栏(折叠/logo/搜索/通知/用户 dropdown) + 可折叠侧栏(菜单源自 `Sys_Module` 递归) + tab pill(可拖拽/右键菜单/中键关闭/iframe 隔离)
- [x] **P0.4 用户栏**：个人资料 / 修改密码 / 切换主题 / 退出
- [x] **P0.5 首页**：`Home/Welcome.cshtml` 工作台（统计卡占位）
- [x] **P0.6 组件库**：btn / input / select / card / tbl / modal / drawer / toast / dropdown / badge / spinner
- [x] **P0.7 业务页过渡**：`_LayoutInner.cshtml` iframe 兼容层，旧 layui 视图零改动可跑

退出条件全部满足：编译 0 错 0 警、登录可用、AppShell 三区渲染、用户菜单可用、折叠/主题持久化、业务页 iframe 加载。

---

## UI · 业务表格统一升级（soul-table / SoulGrid 组件）（2026-05-30）

> 路线：用户验证 Tabulator 试点不满意后，回退 **Layui 2.9.13 + layui-soul-table**，在原生表格上叠加现代能力，并抽出公共组件分批铺开（非"一次性全套"）。

**基础设施（已完成 ✓）**
- [x] Layui 核心降级 2.13.7 → **2.9.13**（兼容 soul-table 筛选，避免 ≥2.11.6 卡死）
- [x] `wwwroot/js/soul-table/`：soulTable / tableFilter / excel / tableChild / tableMerge + css
- [x] 布局 `_LayoutInner.cshtml` 用 `{/}` 绝对路径全局注册 soul-table 模块 + 全局加载 `soulTable.css`
- [x] **公共组件 `wwwroot/js/components/soul-grid.js`**：`SoulGrid.render/export/loadAll/fmtThousand/fmtDate`，默认套用去重筛选(data)+条件筛选(condition)/列显隐齿轮/拖拽/固定列/前端导出/`cache:false`
- [x] 通用列表脚本 `NomalListPage.js` 增加 **`SoulFront` 开关**（前端全量 + soul-table 完整体验）；默认关闭，对其余页面零回归，不无谓加载 excel.js

**已接入页面（去重下拉/固定列/列显隐/排序/前端导出/中文列名/数字千分位）**
- [x] 设备台账 `Facility_ResourceDetail`（前端模式 + 日期范围筛选 + 部门/状态转中文）
- [x] 备件主数据 `Basic_Spare`（NomalListPage `SoulFront` 接入；单价千分位）
- [x] 库存查询 `Spare_NowQuan`（库存/安全库存千分位 + 库存合计行）
- [x] 计量器具 `Meter`（部门/状态转中文）
- [x] 特种设备 `Special_Equipment`（超期红字保留）
- [x] 安全附件 `Safety_Accessory`

**待铺开（按模块分批，大数据表保留服务端分页、慎用前端模式）**
- [ ] 保养项目/模板/工单列表（`Facility_Item` / `Facility_TheTemplateMain` / `Facility_BillMain`）
- [ ] 维修工单列表 `Facility_RepairBillMain`、备件出入库 `Spare_InvoiceMain`
- [ ] 系统管理（用户/角色/部门/菜单）列表
- [ ] 日志/点检历史/状态履历等大数据表（保留服务端分页，去重下拉受限或不启用）

---

## 工作流 借力原则（贯穿所有工作包）

> 后台已部署 工作流（含 webhook、定时触发、AI agent）。以下能力**优先用 工作流 编排**，系统侧只做「配置项 + 调用 + 结果落库/回填」，避免重复造轮子。

| 能力 | URS 关联 | 实现方式 |
|---|---|---|
| **智能解析 / OCR** | 503 / 1002 / 1102 / 1207 / 216 | 前端上传附件 → 调用 `Sys_Setting` 配置的 工作流 webhook → 返回结构化字段 → 前端回填确认。系统不内置 OCR 引擎。 |
| **AI 问答 / 知识库** | 527 / 610 | iframe 嵌入 工作流 AI agent 提供的嵌入式路径，做故障问答/知识检索。 |
| **通知引擎** | 804/805/902/1003/1109/1204/1304/704/607/521/809 | 业务事件(到期/预警/派工) → 写 `Sys_Notify*` 记录 → 触发 工作流 webhook 分发多渠道（邮件/短信/APP-JPush）。 |
| **定时任务** | 维保/检验/校准计划自动生成、到期提醒、催办 | 优先 工作流 定时流；系统暴露受保护 REST API 供 工作流 回调。 |
| **RFID** | 524 / 525 / 526 | 不引入 RFID 硬件，统一按「条码/二维码扫码 + 可手输单号」实现，复用移动端已有 ZXing 扫码。 |
| **电子签名** | 1117 / 1112 / GMP | 封装成熟开源（基于 X.509 证书 + 哈希链/时间戳）为内部签名服务，在校准记录复核、关键审批节点调用，满足 21 CFR Part 11 / EU Annex 11。 |

工作流 相关配置统一进 `Sys_Setting`（webhook URL / API Key / AI agent 嵌入路径），可在「全局设置」页维护。

---

## WP-A 平台底座补强（URS 201-217 / 301-306 / 401-410 / 1401-1406）

> 后续所有模块复用，**最高优先级**。现状：权限/设置/版本/附件已具备；审计仅写入无查询；无通知/工作流/报表引擎。

### A.1 审计追踪
- [x] `Sys_OperationLogController` + 查询页：多条件筛选（用户/记录编号/日期范围/模块）
- [ ] 单条记录变更历史时间线页（旧值/新值/操作理由字段级）
- [ ] 业务写入埋点补全字段级 diff + 操作理由
- [ ] DB 层禁止 `Sys_OperationLog` 的 UPDATE/DELETE（只追加），导出功能

### A.2 用户权限补全
- [x] 账户锁定（锁定记录表 + 列表 + 管理员解锁；失败阈值自动锁定埋点待接登录流程）
- [ ] 会话超时自动注销（可配，默认 15 分钟）
- [x] 用户组（`Sys_UserGroup` + 组-权限，权限叠加并集）
- [x] 登录日志（用户/时间/IP/结果）查询页
- [ ] 预置 8 种角色模板（系统管理员/设备负责人/部门设备管理员/计量管理员/计量员/审核员/批准人/查询员）
- [ ] 2FA/MFA 扩展预留

### A.3 通知引擎（工作流）
- [ ] 新表 `Sys_NotifyTemplate` / `Sys_NotifyRule` / `Sys_NotifyRecord` / `Sys_NotifyPref`
- [ ] 事件 → 规则匹配 → 写记录 → 触发 工作流 webhook 多渠道
- [x] 通知中心页（站内消息 + 已读状态）；顶栏通知入口接真实数据待接

### A.4 工作流引擎
- [x] 新表 `Wf_Template` / `Wf_Node` / `Wf_Instance` / `Wf_ApproveLog`（v2.0.0.sql 已建）
- [x] 通用「提交/审核/批准/派发」线性流程引擎（WorkflowApp：Start/Approve/Reject/Withdraw）；流程模板配置(可视化节点编辑) + 流程审批页(发起/审批时间线)，供设备转移报废、维修、维保、检验、校准、送外检复用
- [ ] 超时升级规则（TimeoutHours 字段已留；定时升级由 工作流 调度，本系统提供数据）
- [ ] 业务模块挂接（在各单据保存/状态变更时调用 WorkflowApp.Start，并以审批结果驱动单据状态）

### A.5 报表引擎 & 通用能力
- [ ] 自定义条件 + 图表（柱/饼/趋势）+ 导出（Excel/PDF/CSV）统一组件
- [ ] 批量导入框架（Excel/CSV + 模板下载 + 完整性/重复性/格式校验 + 导入日志）
- [ ] 数据备份/恢复策略说明 + 恢复测试记录（交付物）

---

## WP-B 设备档案完善（URS 501-532）

> 现状：台账 + Tab 档案 + 附件 + 历史已有。

- [ ] 验收管理 FAT/SAT（`Facility_Acceptance` + 验收问题跟踪表）
- [ ] 设备状态变更审批（转移/报废/封存 → 走 WP-A 工作流引擎）
- [ ] 设备盘点（`Facility_StockCheck`：计划 → 扫码 → 差异 → 导出，支持批量）
- [ ] 标签管理（二维码/条码生成与打印；RFID 按扫码处理）
- [ ] 维修知识库（iframe 工作流 AI）+ 文档版本控制（`Sys_Attachment` 增版本号链 + 旧版本追溯）
- [ ] 证书/许可时效（`Facility_Cert` + 到期 工作流 提醒）
- [ ] 资产卡片（与财务系统对接标注「依赖外部」）、随机配件、润滑标准/记录
- [ ] 智能解析回填（工作流 OCR，准确率目标 ≥95%）
- [ ] 设备分类字典（约 18320 台基础数据）+ 约 20000 份档案初始化批量导入
- [ ] 单台设备档案聚合页自定义导航（基本/采购/配件/润滑/维保/维修/故障/变更/知识库）

---

## WP-C 设备维修增强（URS 601-610）

- [ ] 工单模板（`Repair_Template` 多类型，字段配置）
- [ ] 单工单关联多设备 + 费用分摊（`Repair_BillFacility` / `Repair_Cost`，平均/价值比例/工时权重）
- [ ] 报警规则与记录（`Alarm_Rule` / `Alarm_Record`）→ 异常自动生成预防性维修工单（工作流 / DCS 触发，DCS 标注依赖外部）
- [ ] 故障设备实时看板（维修中/已完成动态更新，完成后自动归档 + 暂停二次报警）
- [ ] 维修统计分析页（按设备/时间/部门）
- [ ] 维修知识库归档（故障现象/原因/分析/方案/备件更换）

---

## WP-D 点巡检重构（URS 701-706）

> 现状：点检偏只读。

- [ ] 巡检标准编制 + 审批（`Inspect_Standard`，走工作流引擎）
- [ ] 巡检计划日历视图（`Inspect_Plan`，推送确认 + 超时升级）
- [ ] 移动端执行补全：扫码 → 记录 → 拍照/视频标注 → 提交（补全现有只读详情）
- [ ] 异常处置分流（立即维修→工单 / 计划检修·停产窗口→维保计划 / 观察监控→监控清单 / 无需处理）
- [ ] 巡检记录查询页
- [ ] 结合历史数据智能优化巡检频率（工作流）

---

## WP-E 设备维保增强（URS 801-809）

> 现状：保养工单较完整（含看板/日历/甘特/派工/状态机/批量生成）。

- [ ] 维保标准独立库（`Maint_Standard`：日常/月度/年度、自维/外委、消耗备件清单）
- [ ] 计划列表三色警示（黄 ≤7 天 / 红超期 / 蓝延期）
- [ ] 延期申请审批（原因 + 新日期 → 审批 → 更新计划，延期后 30 天前再提醒）
- [ ] 资质有效期监控看板（安全合格证/使用许可证/保修证，到期 → 生成维保计划）
- [ ] 按标准自动周期性生成计划 + 到期前 1 个月提醒（工作流 定时）
- [ ] 计划二次编辑/审核/批准/派发完善

---

## WP-F 特种设备管理（URS 901-905）【全新】

- [x] `Special_Equipment`（设备代码/类别/注册登记号/使用证号/设计年限/安全状况等级/下次法定检验日/检验机构 + 自定义扩展字段）
- [x] 检验计划（`Special_InspectPlan`）+ 检验记录（`Special_InspectRecord`：报告/整改/复检）
- [ ] 法定检验计划总览（多维筛选 + 超期红色警示）
- [ ] 60 天 / 30 天分级提醒（工作流）、证书智能识别录入（工作流）
- [ ] 任务分派 / 预约机构 / 上传报告 / 更新下次日期 / 可配置审批归档

---

## WP-G 安全附件管理（URS 1001-1006）【全新】

- [x] `Safety_Accessory`（附件编号/所属设备/型号规格/整定压力/检定范围/最近检定日/周期/机构/状态）
- [x] 检定计划（`Safety_CheckPlan`）；检定记录（`Safety_CheckRecord`）查询/录入页已交付
- [ ] 状态看板（有效期/位置/历史，超期/不合格标红）
- [ ] 检定结果自动同步设备档案
- [ ] 超期/不合格联动特种设备模块 → 一键生成停用/排查工单
- [ ] 到期前 30 天提醒（工作流）、校准证书智能识别（工作流）

---

## WP-H 计量器具管理（URS 1101-1117）【全新 · 17 条 · GMP 合规刚需】

- [x] `Meter`（唯一编号/名称/型号/类别/精度等级/量程/使用部门/存放位置/保管人/状态/校准周期）
- [ ] 计量台账（自定义模板，多维度分类分组）
- [x] 出入库管理（`Meter_InOut` 入库/出库录入；扫码出入库 + 自动更新库存量待增强）
- [x] 校准计划（计划列表/超期红色警示；按周期自动生成待接 工作流 定时）
- [x] 校准记录（标准化电子表单：日期/执行人/规程/环境/标准器/测量数据/不确定度/结论/有效期/证书附件/复核）
- [x] 送外检管理（主子单：申请/审批状态/服务商 + 多选送检器具清单）
- [ ] 批量导入校准证书（Excel/PDF + 证书关联）
- [ ] 多渠道到期提醒（系统/邮件/短信，工作流）、证书智能识别建档（工作流）
- [x] 复核确认后方可生效（校准记录"复核生效"动作）、历次记录追溯
- [ ] 统计报表（柱/饼/趋势 + Excel/PDF/CSV 导出）
- [ ] **电子签名**（21 CFR Part 11 / EU Annex 11，封装开源服务）

---

## WP-I 备件管理增强（URS 1201-1210）

> 现状：主数据/库存/出入库已有（主数据缺编辑保存）。

- [ ] 补 `Basic_Spare` 编辑保存 API + 表单
- [ ] 安全阈值多级预警（库存 < 阈值 → 邮件/短信/看板，工作流）
- [ ] 动态可出库量（库存 - 预约量）
- [ ] 需求计划智能预测（基于故障率/历史消耗，工作流）
- [ ] 备件生命周期跟踪（安装设备/安装日期/更换日期/使用时长/预测剩余寿命）
- [ ] 扫码入库（关联采购合同/供应商）/ 扫码出库（领用人/设备/目的）/ 扫码铭牌 OCR 填充（工作流）
- [ ] 备件盘点 + 备件台账报表（多维检索 + 自定义图表 + 导出）

---

## WP-J 能源管理（URS 1301-1306）【全新 · 采集由 工作流 负责】

> **分工**：数据采集（DCS/PCS7/OPC/Modbus/WinCC）由 **工作流 团队**负责，将结果写入下述存储表。本系统**只负责**：设计存储表结构 + 从库读取结果 + 展示/统计/报警。系统不直接对接采集协议。

- [x] **存储表设计（本系统交付，供 工作流 写入）**：
  - `Energy_Point`（介质类型电/水/蒸汽、计量点编号、仪表型号、协议、采集频率）
  - `Energy_RealtimeData`（时序：计量点/时间戳/瞬时值/累计值）
  - `Energy_Summary`（时/日/月/年汇总）
  - `Energy_AlarmRule` / `Energy_AlarmRecord`
  - `Energy_RunTime`（设备运行时长，按工段/产品）
- [x] 实时监控仪表板（计量点最新值指标卡，数据直接读 工作流 写入的时序表）；自定义布局/曲线待增强
- [x] 能耗多维统计（计量点/维度/时间范围查询）；同比/环比/趋势图待增强
- [x] 报警管理（`Energy_AlarmRule` 规则配置 + `Energy_AlarmRecord` 记录列表/处置）；阈值比对与推送由 工作流 调度写入

---

## WP-K 通用平台 · 移动端 · 合规交付（URS 206-214 / 1501-1609）

- [ ] 响应式适配（PC/平板/手机）
- [ ] 门户仪表盘按角色自定义看板（URS 214）
- [ ] 浏览器兼容（Chrome 主，IE10+ 评估）
- [ ] 性能（页面加载 ≤3s / 查询 ≤5s / 实时数据延迟 ≤3s / ≥50 并发）
- [x] 移动端补全：点检提交(移动端可接单/填写结果/提交)、计量/特种/安全附件扫码查档案(/m/asset 原生扫码+编码查询)、报修流程对齐(报修/接单/开始/完成/三方确认/拍照已具备)
- [ ] 设备初始化批量导入（约 20000 份档案）
- [ ] **需求追溯矩阵**（URS ↔ 功能 ↔ 测试，URS 1601 功能覆盖率 100%）
- [ ] 交付文档（操作手册/部署文档/恢复说明，合并维护，不另建散档）

---

## 跨阶段规范

### 安全 / 协作规范

- 服务器 SSH 访问严格禁止；所有 SQL / 命令由用户手工执行
- 数据库连接 / 密码 / 密钥 / 工作流 API Key 不进公开仓
- 不擅自创建测试脚本 / 辅助脚本
- 不创建 Markdown 文档（**本 ROADMAP 与 URS 模块分析是用户明确要求的例外**）

### 命名 / 编码规范

- 脚本输出统一英文（防跨平台编码）
- 中文允许在 UI、注释、提交信息
- Docker 命令统一 `docker compose`（V2 语法）
- 不主动修改代码，除非用户明确要求

### Git 规范

- 公开仓库：<https://github.com/mli2025/TPM-layui>
- 主分支：`main`
- 提交信息：英文，规范化前缀 `feat / fix / chore / refactor / docs`
- 录入信息后及时提交推送

---

## 下一步（优先级与依赖顺序）

> 全量 URS 12 模块工作量巨大，按「先底座、后业务、合规优先、采集最后」推进。

1. **第 1 档 · 平台底座（WP-A）** —— 审计查询 + 通知引擎 + 工作流引擎 + 报表引擎 + 批量导入。后续所有模块复用，必须先做。
2. **第 2 档 · 已有基础的业务补全（WP-E / WP-D）** —— 维保三色警示·延期·资质·标准库；点巡检标准审批·计划日历·移动执行·处置分流。投入产出最高。
3. **第 3 档 · GMP 合规刚需全新模块（WP-H / WP-F / WP-G）** —— 计量器具（含电子签名）、特种设备、安全附件。法规强相关，须尽早。
4. **第 4 档 · 档案 / 维修 / 备件增强（WP-B / WP-C / WP-I）** —— 验收·盘点·标签·知识库·文档版本；工单模板·费用分摊·报警·看板；备件预警·预测·盘点。
5. **第 5 档 · 能源（WP-J）** —— 依赖采集前置机 / DCS / OPC，放最后，先打通采集再做展示与报警。

每档完成后：编译 0 错 0 警 → 更新 `Sys_Version` 版本记录 → 提交推送 → 更新本 ROADMAP 勾选状态。
